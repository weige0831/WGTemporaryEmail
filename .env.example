# Copy to .env and customize (secrets and compose-level settings only)

# PostgreSQL password used by the tempmail database
DB_PASSWORD=change_this_to_a_strong_random_password

# Host port for the web service (nginx): user panel, admin panel, /docs
# Defaults to 80
WEB_PORT=80
